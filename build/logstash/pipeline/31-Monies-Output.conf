output{
	if [type] == "Monies" {
		elasticsearch {
			template => "/usr/share/logstash/templates/monies.json"
			template_name => "monies"
			hosts => ["${ELASTICSEARCH_URL:elasticsearch}"]
			index => "monies-%{+YYYY.MM.dd}"			
		}	
	}
}